# 🧑‍💻 Practice Problems: Functions & Array Methods

---

## 🟢 Part 1: Functions (Basics)

### 1️⃣ Simple Function

Write a function `greetUser(name)` that:

* Takes a name as input
* Prints: `Hello <name>`

---

### 2️⃣ Add Two Numbers

Write a function `addNumbers(a, b)` that:

* Returns the sum of two numbers

---

### 3️⃣ Even or Odd

Write a function `checkEvenOdd(num)` that:

* Prints `"Even"` if number is even
* Prints `"Odd"` if number is odd

---

### 4️⃣ Square of a Number

Write a function `square(num)` that:

* Returns the square of a number

---

### 5️⃣ Sum of an Array

Write a function `sumArray(arr)` that:

* Takes an array of numbers
* Returns the total sum

---

## 🟡 Part 2: `map()` Practice

### 6️⃣ Add 5 to Every Number

```js
let numbers = [1, 2, 3, 4];
```

Create a new array where each value is increased by `5`.

---

### 7️⃣ Convert to Uppercase

```js
let names = ["salauddin", "ahmed", "rahim"];
```

Create a new array with all names in **uppercase**.

---

### 8️⃣ Multiply Each Number by 2

```js
let values = [10, 20, 30];
```

Create a new array where each value is doubled.

---

### 9️⃣ Length of Each String

```js
let fruits = ["apple", "banana", "mango"];
```

Create a new array containing the **length of each string**.

---

## 🟡 Part 3: `filter()` Practice

### 🔟 Get Even Numbers

```js
let numbers = [1, 2, 3, 4, 5, 6];
```

Create a new array with only **even numbers**.

---

### 1️⃣1️⃣ Numbers Greater Than 50

```js
let scores = [30, 60, 80, 45, 90];
```

Filter numbers **greater than 50**.

---

### 1️⃣2️⃣ Words Starting With "a"

```js
let words = ["apple", "banana", "avocado", "mango"];
```

Filter words that start with `"a"`.

---

### 1️⃣3️⃣ Positive Numbers Only

```js
let nums = [-10, 20, -5, 30, 0];
```

Filter only **positive numbers**.

---

## 🔴 Part 4: `reduce()` Practice

### 1️⃣4️⃣ Sum of Numbers

```js
let nums = [10, 20, 30];
```

Use `reduce()` to find the **sum**.

---

### 1️⃣5️⃣ Find Maximum Number

```js
let nums = [5, 20, 15, 40];
```

Use `reduce()` to find the **largest number**.

---

### 1️⃣6️⃣ Total Price

```js
let prices = [100, 250, 50];
```

Use `reduce()` to find the **total amount**.

---

## 🔵 Part 5: Combination (map + filter + reduce)

### 1️⃣7️⃣ Double Even Numbers

```js
let nums = [1, 2, 3, 4, 5];
```

* Filter even numbers
* Double them using `map`

---

### 1️⃣8️⃣ Sum of Even Numbers

```js
let nums = [10, 15, 20, 25, 30];
```

* Filter even numbers
* Find their sum using `reduce`

---

### 1️⃣9️⃣ Total Length of All Words

```js
let words = ["hi", "hello", "welcome"];
```

* Use `map` to get lengths
* Use `reduce` to find total length

---

## 🟣 Bonus (Interview Level)

### 2️⃣0️⃣ Count Even Numbers

```js
let nums = [1, 2, 3, 4, 6, 8];
```

Use `reduce()` to count how many numbers are even.




